# HEARTBEAT.md — [AgentName] [emoji]

Heartbeat: every [interval]

## Checks (in order)

1. **[Check 1 name]**
   ```
   curl -s "http://localhost:3000/api/issues" | python3 -c "
   import json,sys
   issues = json.load(sys.stdin)
   queue = [i for i in issues if i.get('status')=='[target_status]' and i.get('assignee')=='[agent-id]']
   for i in queue: print(i['task_key'], i.get('priority','?'), i['title'][:60])
   "
   ```
   - For each: [what to do]
   - Transition: PATCH `status=[next], transitioned_by=[agent-id], [required fields]`

2. **[Check 2 name]** (if applicable)
   [Description of second check]

3. Nothing found → HEARTBEAT_OK

## Log
Post to #agent-logs: "[AgentName] heartbeat: [summary of what was done or HEARTBEAT_OK]"

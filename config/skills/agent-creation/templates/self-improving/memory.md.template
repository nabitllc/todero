# [AgentName] Self-Improving Memory — HOT TIER

> Always load at session start. These are learned patterns that compound execution quality.

## Core Mandate
[One-line summary of what this agent does and the most important constraint]

## Workflow Patterns (HOT)
- [Most common task pattern]
- [Common error to avoid]
- [Key command or query]

## Queue Check Command
```
curl -s "http://localhost:3000/api/issues" | python3 -c "
import json,sys
issues = json.load(sys.stdin)
queue = [i for i in issues if i.get('status')=='[target_status]' and i.get('assignee')=='[agent-id]']
for i in queue: print(i['task_key'], i.get('priority','?'), i['title'][:60])
"
```

## Workflow Transitions
- Start: PATCH `status=in_progress, transitioned_by=[agent-id]`
- Complete: PATCH `status=[next_status], transitioned_by=[agent-id], [required fields]`

## What NOT to Do
- [Hard no 1]
- [Hard no 2]
- Never modify MEMORY.md or SOUL.md

## MC API
http://localhost:3000/api/issues

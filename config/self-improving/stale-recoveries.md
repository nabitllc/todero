# Stale Recoveries Log

Auto-generated by `scripts/monitor-stale.py`. Records every issue auto-rolled back from `in_progress` after >2h of inactivity.

Each entry captures: agent, issue type, title, time stuck, and best-guess root cause from prior `implementation_notes`. When the same agent or issue type appears 3+ times, the monitor escalates a pattern note to `self-improving/memory.md`.


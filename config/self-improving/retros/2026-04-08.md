# Retro — 2026-04-08

## Summary
Pre-migration sprint review before kaos-config rename.

## What Went Well
- Rejection-aware spawn prompt shipped
- Port topology consolidated to single next dev process

## What Needs Work
- Hydration mismatches persisting (reverted fixes)
- Ops queue draining faster than PO can refine

## Repeated Patterns
- Builder regenerates same defective code on re-pick without rejection context
- Stale worktrees block new spawns with branch collision errors
- Missing env vars cause silent runtime failures — use fail-loud imports
- Worktree collisions when stale branches not cleaned up
- PO refinement lag causes ops queue starvation

- 2026-04-04: Canonical issue status-category naming is now enforced in repo as `Planned / Ongoing / SignOff / Done`; API code, UI/reporting/tests, and migrations were updated to align, with remaining risk primarily in live Supabase state and external n8n/scripts.
- 2026-04-04: Shared lifecycle helper logic was added in repo to treat `completed`, `released`, and `closed` as real finished states instead of retired `done`; key server paths, tests, queue config, and tester script were updated accordingly.
- 2026-04-04: Michael clarified that if `Defined` is kept, it should not exist redundantly as both a status and a field; recommendation converged on replacing feature `defined` status with a definition/readiness field in a future workflow redesign.
- 2026-04-04: Recommendation for workflow architecture is to separate issue type from workflow family so multiple types can share one workflow; specifically, `ops` should likely reuse the same execution workflow family as `task` rather than maintaining a divergent hardcoded lifecycle.
- 2026-04-04: Remaining follow-up area after repo cleanup is production correctness: apply updated lifecycle/category migrations to live Supabase, audit live data for retired statuses/null categories, inspect n8n/external scripts for old lifecycle writes, verify end-to-end on real issues, and scrub stale issue specs that still encode retired lifecycle wording.

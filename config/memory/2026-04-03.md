- Michael asked that workflow documentation include not just transitions/validators/post-functions, but also the fields used by the workflow system, including whether each field is required vs optional and any allowed option sets.

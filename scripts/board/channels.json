{
  "goalAvg": 8.9,
  "channels": [
    {
      "name": "Identity, Auth & Access Control",
      "baseline": 0.5,
      "current": 7,
      "goal": 8,
      "evidence": "All five authorization checks pass, and ?all_projects=1 no longer defeats the task_key scope boundary — a client-controlled query string had been able to read any issue by key across projects, defeating the 404 whose stated purpose was preventing exactly that. Verified: scoped lookup of a foreign key returns 404 with and without the param, while a scope-blind script still resolves it. Held back from the goal because authentication is still three shared passwords doubling as their own session tokens — no users, no logout, no rate limiting.",
      "goal_text": "builderz-labs — per-agent API keys and viewer/operator/admin roles over real sessions, replacing three shared passwords that double as their own tokens.",
      "wave": null
    },
    {
      "name": "Work Management API (MC API)",
      "baseline": 2,
      "current": 8,
      "goal": 9,
      "evidence": "The code-review lifecycle completes AND the bug lifecycle is unblocked. `environment` was required by a validator and was not a column, so a bug moving defined->open answered 400 without it and 500 with it — every bug in this installation was stuck, in both directions, and the Bug Details panel read \"No bug details provided\" on every bug ever opened. Now 200 with the fields stored. A body carrying the phantom column returns 422 NAMING the real columns rather than 500, which matters because lib/agent-queue.ts still instructs the Tester agent to send it — the outage was one agent run from returning. The MC API also no longer routes work to agents that do not exist. Held back: limit=0 still means every matching row and two callers use it to read one integer, and the API still returns raw CHECK constraint text for an invalid enum value.",
      "goal_text": "Linear's issue list — server-side paging with an exact total, so no screen ever shows a number computed from a truncated slice.",
      "wave": null
    },
    {
      "name": "Portability & Host Independence",
      "baseline": 0.5,
      "current": 8,
      "goal": 9,
      "evidence": "npm install really was broken, on the owner's own primary goal, at step one. This host genuinely has no C++ toolchain, so a fresh copy failed the way a new user's would: gyp ERR! find VS. Not simulated. The builder traced the cause into npm's arborist source rather than guessing — the synthetic node-gyp rebuild step gates on pkg.gypfile !== false, and lockfileVersion 3 has no slot for gypfile, so resolving from the lockfile always reads undefined there whatever better-sqlite3's own manifest says. With the field added: install exit 0, npm ci exit 0, prebuilt binary used, no build directory. The better half is the durability gap it then found — a plain npm install silently strips that field back out on its next write — so there is a test, and I proved it myself by removing the field and watching it fail BY NAME, then restoring. A full clone-to-running trial was run TWICE from fresh copies: install, setup, doctor, dev, /api/health, login, an unauthenticated 401, then an authenticated create and read. HELD BACK: macOS and Linux are unverified, npm run build and npm start were not exercised (build is forbidden on this host), and a genuinely reachable Postgres was never tested — only the dead-endpoint path.",
      "goal_text": "builderz-labs — SQLite and one start command, no Redis, Postgres or Docker. Clone it, run it, working app on any OS with no cloud account.",
      "wave": 7
    },
    {
      "name": "LLM Provider Independence",
      "baseline": 1,
      "current": 8,
      "goal": 9,
      "evidence": "This channel carried a FALSE evidence string for several waves: \"66 remaining references — chat still routes to a cloud host the owner has ruled out\". A builder measured it and both halves are wrong. Live code across app/lib/components/hooks contains ZERO references to the ruled-out provider, comments stripped or not. Chat routes to LLM_BASE_URL, which is http://localhost:11434/v1 — proven live rather than read: a cloud model id returns 400 naming the local endpoint it is not present in, and a local qwen2.5-coder id returns 200 with an SSE stream. The model list is proxied from that endpoint, so it is live rather than hardcoded, and a new test locks the dangerous shape — an unreachable or empty roster must error naming the URL and must NOT fall back to a constant. The provider is genuinely a base URL: selection is an env var, resolveProvider refuses to default to a cloud host, and tiers resolve against the live roster rather than a static map. A new guard derives its banned set from the \"Never X\" bullets in HANDOFF.md rather than carrying its own list. Held back: two migration CHECK constraints still ADMIT a ruled-out connection type on a legacy table with no callers, and two CLI runtimes map tiers to hardcoded vendor ids because their provider is a binary, not a base URL — there is no seam to move them onto.",
      "goal_text": "LiteLLM — the provider is a base URL, not a code branch. Open WebUI — the model list is read live from that endpoint, never hardcoded.",
      "wave": null
    },
    {
      "name": "Agent Visualization Fidelity",
      "baseline": 3,
      "current": 5,
      "goal": 9,
      "evidence": "The Office states '0 of 30 agents report a workspace folder — the positions on the floor below are decorative', computed from the field so the wording self-corrects if agents ever report one. That is the honest version of a surface that previously implied position meant something. Held back: GET /api/tasks 404s on every poll from two independent pollers, 30s and 60s, and the roster still lists Kemuni SME and Vespera SME — agents for projects that do not exist.",
      "goal_text": "pixel-agents — every character state derives from real Claude Code hook events, and a speech bubble appears when an agent is waiting on you.",
      "wave": 6
    },
    {
      "name": "Data Layer & Schema Management",
      "baseline": 0.5,
      "current": 8,
      "goal": 9,
      "evidence": "Migration 066 restores four columns that a live validator required and the schema did not have — environment, steps_to_reproduce, expected_behavior, actual_behavior — and DELIBERATELY does not add test_status, with both halves of that reasoning in the migration comment. The evidence for the split was measured, not argued: across 3,078 production rows in the Supabase export the stored test_status disagreed with the derived value on 372 of them, 12.1 percent, with passed sitting on rows whose tester and designer were both still pending. A stored duplicate of a derived value that was already wrong one time in eight. A new no-phantom-columns guard compares every column named in live code against the LIVE schema and is proven in both directions across writes, filters, selects and query strings — and states in its own header the case it would NOT catch, which is the interface declaration where this defect lived longest. WAVE 6 KEPT THIS AT 8 RATHER THAN RAISING IT, because a dialect asymmetry sat in the seam the whole time: an equality filter on a schema-declared BOOLEAN column bound a string, which Postgres infers correctly for free and SQLite never matches. Every Postgres-run test passed. The seam's two-dialect discipline is exactly what this channel scores, and it had a hole only the host a fresh clone actually runs could see. WAVE 7 closed the gap the previous fix could not reach and added a ninth guard for the class. Three columns are declared boolean in postgres and plain INTEGER in their sqlite twins, so the type-keyed hook was blind to them; no filter targets them today, so what existed was a trap whose trigger was 'somebody writes an ordinary filter'. The builder chose a curated override map over rebuilding two hot-path tables that every live agent-runtime wrapper writes to continuously, and deliberately did NOT leave a migration file on disk where an unattended db:migrate could pick it up before a maintenance window exists. I proved the new guard both ways myself, and — because this repo has twice shipped a guard that ran nowhere — confirmed the smoke test exits 1 with a gap planted and 0 without. Also measured this wave: the eq coercion works INSIDE an or(...) clause, which a critic had flagged as possibly a separate parse path.",
      "goal_text": "builderz-labs — migrations run at first DB access, so starting the app creates the schema. No migrate command to know about, no colliding prefixes.",
      "wave": 7
    },
    {
      "name": "Observability & Honest Reporting",
      "baseline": 1,
      "current": 8,
      "goal": 9,
      "evidence": "Seven guards now — invented-projects, unscoped-issues, no-secrets, silent-empty, dead-modules, cloud-provider, phantom-columns — and every one proven in both directions. Two more landed this wave: cloud-provider derives its banned set from the \"Never X\" bullets in HANDOFF.md so it cannot drift from the decision record, and phantom-columns compares code against the live schema. The most valuable output was a FAKE GREEN caught in the one tool a newcomer runs first: npm run doctor printed a raw Require stack dump, then said \"OK — this host can run Todero\" and exited 0, while health on the same checkout reported db.reachable false. It resolved the provider NAME from env without ever touching the driver; it now opens an in-memory database and round-trips a row. Held back: each new guard names a real blind spot in its own header rather than implying coverage it does not have. WAVE 7 FOUND A VIOLATION INSIDE THE TOOL WHOSE ENTIRE JOB IS HONESTY, and it stays at 8 because of what is still open, not because nothing moved. `npm run doctor` printed 'OK — this host can run Todero.' and exited 0 against a CORRUPTED database and against a Postgres URL pointed at a closed port. Both reproduced, then fixed, then verified by me independently: corrupt sqlite exits 1 naming the PRAGMA quick_check failure, dead Postgres exits 1 naming connect ECONNREFUSED, the real database still exits 0. While fixing it the builder found a second bug in its own path — AggregateError.message is empty, so the failure had been printing a blank line where the cause belonged. Separately, Settings had been rendering '🗄️ Supabase — 0% used — Free Tier — 952.0 KB / 500.0 MB' on an install running SQLite: three fabrications in one tile, since the 500 MB is a vendor plan number applied to a local file with no limit and the percentage is computed against that invented denominator. Measured in a browser after: '🗄️ SQLite — No size limit — Local file — no vendor size limit — 952.0 KB'. HELD BACK: lib/llm-provider.ts still silently defaults a JSON-parse failure to an empty model list, so a server that is not OpenAI-compatible reports the IDENTICAL 'reachable, 0 models' a real Ollama with nothing pulled reports — two very different problems, one indistinguishable answer. A shape probe now tells them apart at diagnosis time, but the silent default is still in the product path.",
      "goal_text": "Paperclip — full tool-call tracing and an audit log per ticket. LangSmith — drill from a cost number to the run and step that caused it.",
      "wave": 7
    },
    {
      "name": "Work Management UI",
      "baseline": 1,
      "current": 7,
      "goal": 9,
      "evidence": "A fresh-context critic re-measured all 16 Pipeline destinations from fresh fixtures and found 15 of 16 offered and 15 of 16 completing — but the one DISABLED destination was the one the server accepts, and it is the gesture a board user makes most: sending a card back to backlog. It rendered greyed with \"reserved for main, po, ops, and you are signed in as michael\" while app/api/issues/route.ts returned 200 for exactly that PATCH (measured twice, ~40 min apart) and said so in its own refusal string. The false claim was defended in three places — the roles list, a comment reading \"Measured: defined -> backlog as the owner is 403\", and a test that turned RED when the code was corrected. Fixed together with its mirror-image defect, because fixing either alone trades a false refusal for a false promise: moveVerdict returned `ready` for a sprint-carrying card whose move 500s on the backlog/sprint CHECK; moveBody now clears the sprint (measured 200 with sprint:null, 500 without). Owner recognition now imports isOwnerActor from lib/operator-identity.ts so that half cannot drift again. Score unchanged at 7 deliberately: the fixes are real, and they also revealed that the previous 7 rested on a claim nobody had re-measured.",
      "goal_text": "Linear — a board that refuses a move the server rejected, and never renders an empty state over an error.",
      "wave": 6
    },
    {
      "name": "Agent Fleet Operations",
      "baseline": 0.5,
      "current": 7,
      "goal": 9,
      "evidence": "Liveness carries PROVENANCE now, not just a value. A registration with no check-in inherited its creation time as lastSeenAt and Fleet worded it as a hook event — measured with the heartbeat store readable and empty: 'last event 4m ago' over zero events. AgentDto now carries lastSeenSource, each builder sets it from the branch it took, and the classifier returns never for any non-heartbeat source — the same refusal it already made for an unread store. CrewTab fails CLOSED when the field is missing, and /api/connect stops discarding the heartbeat write warning. Four states, all reachable, all sourced.",
      "goal_text": "builderz-labs — POST /api/connect registers an agent and returns the URLs to use next; 30s heartbeats, offline at 10min. Liveness from data, never an array.",
      "wave": 4
    },
    {
      "name": "Run Safety & Enforcement",
      "baseline": 0,
      "current": 7,
      "goal": 9,
      "evidence": "A second layer of protection had been silently off and nothing said so. An equality filter on the blocked flag matched ZERO rows on the live SQLite host — no error, an empty result indistinguishable from 'there are none' — and that filter is ANDed into the eligible-issues query for EIGHT OF NINE agent-queue lanes, is main's entire blocked-issue triage filter, and sits in four WIP-ceiling counts. A ceiling whose count always reads 0 never fires. It hid because the is-filter parses to a real boolean while the eq-filter stays a string, so the two read as synonyms; and because Postgres infers the parameter type for free, so every Postgres-run test passed while SQLite — what a fresh clone runs — compared TEXT to INTEGER and never matched. Fixed at the seam rather than the call sites, on the SQLite flavour only, keyed off the schema-declared column type so a text column genuinely holding the word 'false' is untouched; proven red-then-green on both dialects and over real HTTP. The dispatch kill switch was never affected and both of its acceptance checks still pass. HELD BACK, and stated rather than fixed: three more boolean-meaning columns are declared INTEGER rather than BOOLEAN in the sqlite migrations, so they would reproduce this exactly the moment a query-string filter targeted one, and this fix cannot catch them because it keys off the declared type. Also the ceilings have never been observed FIRING against a real dispatch run, because dispatch is off by owner directive.",
      "goal_text": "Paperclip — budgets per agent, and at the limit they stop. Portkey — the cap is enforced in the request path, outside the agent, so it cannot be reasoned past.",
      "wave": 6
    },
    {
      "name": "Learning & Memory Loop",
      "baseline": 1,
      "current": 7,
      "goal": 9,
      "evidence": "The chain was already built — my briefing said 'nothing populates the store' and that was WRONG, the ninth wrong briefing this session; all four runtime adapters already called the exit hook and 37 tests were green. What was true is that the table held zero rows, so it had never been EXERCISED. A critic reproduced it end to end with its own vocabulary: real API transitions setting rejection_count by the app's own rule, a real spawn, a real row written on process exit, then retrieval on a task key that never existed, matching on FTS5 vocabulary alone. It also built a PGlite harness nobody had run and proved the Postgres keyword-overlap fallback is REAL, not a stub, with an explicit incomplete-scan flag so a bounded scan cannot masquerade as a complete search — it scored that as BEATING the competitor, which is SQLite-only. THE GAP IT FOUND: the exit hook never selected the issue title, so every lifecycle row carried a null title — and both engines index that field. A run finishing CLEANLY, with no rejection, wrote a row whose only searchable content was a task key the ranker deliberately strips: an unsearchable record. Fixed and pinned by a test that retrieves a clean run by title alone. A budget overflow now surfaces as 503 with its token numbers instead of an unstructured 500, with the claim reverted so an issue does not sit claimed with nothing running. HELD BACK: what was attempted, whether it succeeded, and the exit status are still null on every lifecycle row — deferred deliberately, since no real exit code is available at that call site — so a record says what was rejected, not what was tried. And the loop never closes automatically here because dispatch is off by owner directive.",
      "goal_text": "Hermes Agent — records what it tried and failed after each task, retrieves it on similar ones by FTS5, and caps in-context memory so overflow errors instead of truncating.",
      "wave": 6
    },
    {
      "name": "Navigation & Deep Linking",
      "baseline": 1,
      "current": 7,
      "goal": 8,
      "evidence": "An issue is fully addressable now and I verified every claim in a browser myself, because three agents this wave reported having no browser tool and said so rather than substituting a code-read. Measured after the seam diffs landed: a permalink survives a full reload; navigating to Fleet with an issue open leaves ZERO dialogs, where before the overlay stayed mounted over a screen whose address bar named something else; closing leaves history.length at 50 from 50, where before it PUSHED so Back re-opened the issue you had just closed; and a malformed key renders 'notakey not found in Limiglow' instead of silently rewriting the address bar so a typo'd shared link looked like an ordinary page load. The guard that makes all this survive is finally reachable by a test: a critic had mutated it, destroyed the feature, and watched every gate come back byte-identical green. The decision moved into a pure function where 35 tests can reach it, and I re-ran that exact mutation myself — 2 fail BY NAME, 35/35 on revert. WHAT KEEPS THIS BELOW GOAL, and it is named in the goal text: 'LangSmith — every trace has a URL.' Runs has ONE view, 'all', and an individual run has no address at all. Verified by reading components/nav/config.ts. The issue half of 'no screen unreachable' is done; the trace half does not exist, and that is the next piece rather than a rounding error. Also still true: app/page.tsx itself cannot be rendered by any test here — jest runs testEnvironment node, jest-environment-jsdom is not installed, and parseURL is not exported.",
      "goal_text": "Slack — every message has a permalink that opens it in context. LangSmith — every trace has a URL. Linear — any view is a link. No screen unreachable.",
      "wave": 7
    },
    {
      "name": "Approval & Human-in-the-loop",
      "baseline": 0,
      "current": 7,
      "goal": 9,
      "evidence": "Every refusal path now reaches the operator. ApprovalCard claimed a label could not promise an effect the server would refuse; the check it cited compares type-key sets only, and two of four live cards offered a button the server answers 409 to. approveGate runs the route own preflightDecision and shows the server reason VERBATIM for every code, disabled rather than absent. The audit trail records refusals and failed effects as failed. Held back: decided_by is still a client-side literal while mc-auth exists, and requestApproval() has no callers, so approving repairs rather than gates.",
      "goal_text": "Slack — the difference between new, for-you, and a thread you are in. Grok Bot — surfaces only what needs you. You approve from your phone; Todero sends.",
      "wave": 4
    },
    {
      "name": "Pipeline Fidelity",
      "baseline": 2,
      "current": 7,
      "goal": 9,
      "wave": 6,
      "evidence": "All 16 lifecycle statuses map across 8 columns, each exactly once, as data a test enumerates — and the completeness test is red in both directions: unmapping a status fails 5 of 53 saying an issue in it would be invisible, inventing one fails 3 of 53 naming it. The lane by agent is real: 29 lanes from 28 live roster rows, with the 22 idle agents each still named, so an agent with nothing to do is distinguishable from one that does not exist. A smoothing pass then found BoardTab — the DEFAULT Work view — dropping six statuses silently: header reading \"19 issues\" over columns holding 12, with no warning that seven cards had gone. Not hypothetical: the MC API writes the status \"underway\" on two paths, so it happens in normal operation. Its three-column simplification stays, but its catch-all chip is now DERIVED from VALID_STATUSES minus everything already placed, so a status added to the lifecycle tomorrow appears rather than vanishing. Verified zero uncovered. Held back: 7 of 15 move-sheet transitions still cannot complete, two of them surfacing raw SQLite constraint text to the operator. THIS WAVE: the move sheet's submit button could not recover from a failure. setSubmitting(true) had no path that could ever clear it — onSubmit was typed `(values) => void`, the async onMove promise was never awaited, and TypeScript accepts a Promise-returning function for a void-returning type, so tsc never complained. Reproduced against the real endpoint with a real 409 lane refusal: the button read \"Moving…\", permanently disabled, correct error above it, no way to retry without closing the sheet. Now awaited, with a mounted-ref guard so a success does not flicker the button before the sheet closes; a second submit re-sends (fetch count 1 -> 2, measured).",
      "goal_text": "Linear — group a board by any dimension, every column a real status. Plane — swim lanes. Todero adds a lane per agent, so you see what each one is working through."
    },
    {
      "name": "Customer Conversations",
      "baseline": 0,
      "current": 7,
      "goal": 9,
      "wave": 7,
      "evidence": "The transport SEAM exists; the transport does not, and that is deliberate — the provider is an open decision on this board and no agent was allowed to pick one. What landed: a provider-agnostic inbound webhook with its OWN dedicated credential, idempotency (a real provider retries, and a repeat delivery now answers 200 duplicate with the original row instead of creating a second customer message), an outbound adapter interface with ZERO implementations that refuses an unapproved draft both at runtime and by type, and reply-level threading inside a conversation — migration 070, both dialects. The builder re-measured all four 'already good' claims instead of assuming them, including re-running the approve-gate CHECK constraint in both dialects and the draft-cannot-approve-its-own 201-then-403 against the live server. It also found a structural limit and refused to work around it: middleware gated every /api/* route on a session or the GENERAL internal secret before any handler ran, so its dedicated credential was correct and still unreachable without also knowing Todero's general secret. It wrote the diff and left the shared file alone. I applied it only after measuring all three refusals myself — no auth 401 from middleware, wrong secret 401 from the route saying 'refused, not passed through to any other check', and an UNCONFIGURED secret still refusing rather than becoming a bypass. Card verified in a browser: 'Who is waiting on a reply? / 0 awaiting approval' with the query printed. HELD BACK: no provider, so an outbox that cannot send is still a draft folder; and the VALID webhook path is proven at the route level only — the env var is unset here and restarting the server is forbidden.",
      "goal_text": "Slack — a reply threads instead of drowning the channel. Grok Bot — drafts while you are away, surfaces only what needs approval. You approve; Todero sends."
    },
    {
      "name": "Commerce Operations",
      "baseline": 0,
      "current": 7,
      "goal": 8,
      "wave": 6,
      "evidence": "Both writers of inventory_levels are now compare-and-swap, not one. The hardening had been applied to the PATCH handler while the ORDER FULFILMENT path — the one that ships goods — was still read-modify-write with no error check. Fixing it surfaced a defect no critic had named: the order's own fulfilment_status write was unconditional too, so two concurrent requests could both pass the transition check and both decrement stock. A DOUBLE SHIP. Measured after the guard: 10 concurrent fulfils gave 1x200 and 9x409, stock moved exactly once. The audit insert had discarded its error — a critic forced it and moved 7 units of stock with HTTP 200 and zero audit rows, ledger and stock permanently diverged; the fix was already written 25 lines away in the same directory and had not been ported. Real commerce permissions replace borrowed project ones on all 8 handlers, refused and permitted both ways, additive with no other route's authorization changed. AND THE HEADLINE MEASUREMENT WAS AN ARTIFACT: the claimed '20 successes, zero conflicts' came from backgrounded curl processes that never overlapped on Windows; with real single-process concurrency the pre-fix race lost 18 of 20, not 1. Re-measured at 20/30/50/80-way — every run exact, real refusal rates now stated rather than hidden. HELD BACK: no provider integration (needs an owner decision), no partial fulfilment with per-line quantities, an 8-attempt ceiling that refuses 55% of writes at 20-way which is honest but not usable as a bulk-sync target, and no cross-table transaction in the db seam on either dialect.",
      "goal_text": "Shopify admin — orders, catalog and inventory as first-class objects you can act on, not a dashboard that only reports what happened."
    },
    {
      "name": "Multi-tenancy & Identity",
      "baseline": 0,
      "current": 1,
      "goal": 8,
      "wave": 9,
      "evidence": "Still deliberately post-MVP per HANDOFF — authentication remains three shared passwords and every record is global. Two things moved: GET /api/agent-responsibilities stopped answering 200 with a full payload for any business_id (now 404 unknown_hub), and project scope resolution for conversations and commerce collapsed into one lib/scope.ts whose surface descriptor is DATA rather than strategy — it can change what a refusal is called, never whether there is one. The channel stays near zero until real sessions and per-tenant isolation exist.",
      "goal_text": "Linear — sign in and see only your workspace. Anyone runs Todero for their own business and projects, isolated from everyone else."
    },
    {
      "name": "Secrets & Credential Custody",
      "baseline": 1,
      "current": 7,
      "goal": 9,
      "wave": 7,
      "evidence": "Custody stopped being decorative. The operator could add a Discord credential to a hub and see it stored, and exactly ONE of the app's send paths consulted it — every other notification came from a process-wide env var. Counting it took three tries: a builder reported seven direct env reads, I corrected it to eight after grepping myself, and the next builder found NINE across seven files, because status/route.ts has two and everyone had counted it as one. All nine now resolve through lib/discord-sender.ts, which wraps resolveHubDiscord, keeps the env var as a documented fallback, and refuses BY NAME when neither exists. One site had been doing console.error('message dropped') — a silent drop from the operator's side, since nobody watching the app ever learned it had not gone. Two sites deliberately do NOT thread a hub id because none is in scope there, and the code says so rather than implying resolution it does not do. I read the app/api/issues/route.ts diff line by line myself — it has broken production twice this session — and it touches postDiscord and notifyWatchers and nothing else; grepping it for isOwnerActor, KAOS_ROLES, scope, transition, validator, permission, backlog and archived returns one hit, the word 'scoped' inside a new comment. HELD BACK HARD: the OpenRouter key is still live in origin/main's history until the owner rotates it. And only ENV custody was exercised live — stored/encrypted custody could not be, since the encryption key is unset in this process.",
      "goal_text": "Doppler / Vercel env — secrets encrypted at rest, scoped per service, revocable without redeploy, and never printed. Todero holds delegated tokens only."
    },
    {
      "name": "Search & Findability",
      "baseline": 1,
      "current": 8,
      "goal": 8,
      "wave": 7,
      "evidence": "CLEARED. Enter opens the issue itself rather than landing on a destination default, results open in context over the surface that would otherwise list them, and the modifiers narrow to the one you meant. A critic scored in: as BEATING Slack — it validates against all 16 real statuses, names them all on failure, and fires ZERO network requests on a bad value, where Slack simply returns nothing. before: had been a shape regex that accepted 9999-99-99 and 2026-13-45, issued a live query and rendered results under 'issues matching this filter'; calendar-impossible dates are refused now. from: was case-sensitive and unvalidated, so a capitalised name returned 'no issue matches', indistinguishable from 'that person has no issues'. And the filters now live in the URL via replaceState, which was the remaining half of Linear's 'any view is a link' — they had been held in the palette's local state and lost on close. Every verb the palette offers was executed against the live API: 9 offered all PATCH 200, all 6 withheld ones genuinely refuse, and withheld moves now show their real reason instead of vanishing. WHAT KEEPS IT AT 8 RATHER THAN HIGHER: from: is case-insensitive but still not validated against the real assignee roster, so a name that does not exist reads as a person with no work — Slack will not let you type a non-existent user without seeing it. That is the one place this still loses.",
      "goal_text": "Slack search — modifiers like in:, from:, before: that narrow 2,440 records to the one you meant, and results that open in context rather than a dead list."
    }
  ]
}